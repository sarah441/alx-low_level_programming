#include "main.h"

/**
 * int_abs is the integer to be checked
 *
 * Return: The absolute value of the integer.
 */
int _abs(int)
{
	if (c < 0)
	{
		int abs_val;

		abs_val = c * -1;
		return (abs_val);
	}
	return (c);
}
